# car-price-prediction
from a car price vs ( Mileage, Age and Car Model ) dataset, i have trained the model LinearRegression of sklearn.linear_model to predict the car price from ( Mileage, Age and Car Model ) data input
used pd.get_dummies for Car Model data.
check the score of the model using model.score().